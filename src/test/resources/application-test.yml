spring:
  datasource:
    username: admin
    password: 123456
    url: jdbc:h2:mem:academic
    jpa:
      database-platform: org.hibernate.dialect.H2Dialect

api:
  security:
    token:
      secret: ${JWT_SECRET:my-secret-key}